{
  "name": "goldmandu",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "scripts": {
    "dev": "node scripts/sync-data.mjs && astro dev",
    "build": "node scripts/sync-data.mjs && astro build",
    "preview": "astro preview",
    "sync-data": "node scripts/sync-data.mjs",
    "build:site": "node scripts/sync-data.mjs && astro build"
  },
  "dependencies": {
    "chart.js": "^4.5.0",
    "water.css": "^2.1.1"
  },
  "devDependencies": {
    "astro": "^5.12.0"
  }
}
